# Multi-Source Candidate Data Transformer

Normalizes, merges, and outputs candidate profiles from a recruiter CSV
(structured) and a GitHub profile (unstructured) into a canonical schema,
with configurable projection, provenance, and confidence scoring.
